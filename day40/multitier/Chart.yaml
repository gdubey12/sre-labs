apiVersion: v2
name: multitier
description: A multi-tier app - frontend (nginx) + backend (httpbin) + Redis cache
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: redis
    version: "27.0.4"
    repository: "https://charts.bitnami.com/bitnami"
    condition: redis.enabled
