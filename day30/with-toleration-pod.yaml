apiVersion: v1
kind: Pod
metadata:
  name: with-toleration-pod
spec:
  tolerations:
  - key: "dedicated"
    operator: "Equal"
    value: "monitoring"
    effect: "NoSchedule"
  containers:
  - name: nginx
    image: nginx:alpine
