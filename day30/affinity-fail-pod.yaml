apiVersion: v1
kind: Pod
metadata:
  name: affinity-fail-pod
spec:
  tolerations:
  - key: "dedicated"
    operator: "Equal"
    value: "monitoring"
    effect: "NoSchedule"
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: disktype
            operator: In
            values:
            - nvme
  containers:
  - name: nginx
    image: nginx:alpine
